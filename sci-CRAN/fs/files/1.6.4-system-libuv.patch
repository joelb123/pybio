diff --git a/src/Makevars b/src/Makevars
index b98c7c8..74fb6ea 100644
--- a/src/Makevars
+++ b/src/Makevars
@@ -2,7 +2,7 @@ UNAME := $(shell uname)
 
 LIBUV := libuv-1.44.2
 
-PKG_LIBS = ./$(LIBUV)/.libs/libuv.a -pthread
+PKG_LIBS = -luv -pthread
 
 SOURCES = $(wildcard *.cc unix/*.cc)
 OBJECTS = $(SOURCES:.cc=.o)
@@ -21,24 +21,15 @@ ifneq ($(filter $(UNAME), Linux AIX OS400),)
 OBJECTS +=  bsd/setmode.o bsd/strmode.o bsd/reallocarray.o
 endif
 
-PKG_CPPFLAGS = -I./$(LIBUV)/include -I. -pthread
+PKG_CPPFLAGS = -I. -pthread
 
-all: $(SHLIB)
+all:
+	rm -r $(LIBUV)
 
-$(SHLIB): $(LIBUV)/.libs/libuv.a
-
-$(LIBUV)/Makefile:
-	(cd $(LIBUV) \
-	&& CC="$(CC)" CPPFLAGS="$(CPPFLAGS)" CFLAGS="$(CFLAGS) $(CPICFLAGS) $(C_VISIBILITY) -std=c99" AR="$(AR)" RANLIB="$(RANLIB)" LDFLAGS="$(LDFLAGS)" ./configure $(R_CONFIGURE_FLAGS) --quiet)
-
-$(LIBUV)/.libs/libuv.a: $(LIBUV)/Makefile
-	$(MAKE) --directory=$(LIBUV) \
-		HAVE_DTRACE=0
 
 .PHONY: shlib-clean clean
 
 # shlib-clean: clean
 
 clean:
-	$(MAKE) --directory=$(LIBUV) distclean
 	rm -f $(OBJECTS)
